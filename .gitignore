node_modules/
.env
.env.*
.DS_Store

# Local-only secrets / scratch. Never commit — this repo is public.
workspace/
*.token
secrets*

# Supabase CLI scratch state — machine-local, not part of the project.
supabase/.temp/
supabase/.branches/
